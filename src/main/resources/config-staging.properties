# ============================================================
# Staging Environment Configuration
# ============================================================
# Values here override config.properties when env=staging
# ============================================================

base.url=https://staging.example.com
browser=chrome
headless=true
explicit.wait.timeout=15
thread.count=2
screenshot.on.failure=true
retry.count=2
env=staging
ci.mode=false
