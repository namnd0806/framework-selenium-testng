# ============================================================
# Production Environment Configuration
# ============================================================
# Values here override config.properties when env=production
# ============================================================

base.url=https://www.example.com
browser=chrome
headless=true
explicit.wait.timeout=20
thread.count=4
screenshot.on.failure=true
retry.count=1
env=production
ci.mode=false
